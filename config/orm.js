// require the connection for the ORM from the connection file.
var connection = require("../config/connection.js");

// object for all of our mySql statement funcitions 
// creating the ORM

var orm = {
    selectAll: function() {

    },

    insertOne: function() {

    },

    updateOne: function() {

    }
}

// export the orm object for the model (burger.js)
module.exports = orm;

